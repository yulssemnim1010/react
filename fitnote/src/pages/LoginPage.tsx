import { ScreenFrame } from "../components/ScreenFrame";

export function LoginPage(){
  return(
    <ScreenFrame label="로그인" backgroundSrc="" glowVariant="login">
      <div>로그인</div>
    </ScreenFrame>
  )
}